# چت‌بات پیشرفته آیلتس

این پروژه یه چت‌بات متنی برای پاسخ به سوالات آیلتسه که با مدل DistilBERT فاین‌تیون شده و با Flask به صورت API اجرا می‌شه.

## پیش‌نیازها
- پایتون 3.8 یا بالاتر
- دسترسی به اینترنت (برای دانلود مدل‌ها)
- (اختیاری) Google Colab برای آموزش با GPU رایگان

## نصب
1. یه محیط مجازی بسازید (اختیاری):
   ```bash
   python -m venv venv
   source venv/bin/activate  # لینوکس/مک
   venv\Scripts\activate  # ویندوز